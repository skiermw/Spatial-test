set schema = 'SHELTRT';
select 
   	policy_no, 
	agent_no, 
	line_id, 
	insured_nm,
	family_no, 
	status_cd, 
	total_premium_am, 
	tier_cd, 
	ins_address,
	ins_city,
	ins_state
from
 	pol01_policy
where 
   	ins_zip_cd = '65203' AND status_cd = 'A'
fetch first 200 rows only
with ur;

