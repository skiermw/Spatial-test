set schema = 'SHELTRT';

select 
   	strip(policy_no,BOTH),',', 
	strip(agent_no,BOTH),',', 
	strip(line_id,BOTH),',',
	strip(insured_nm,BOTH),',',
	strip(family_no,BOTH),',', 
	strip(status_cd,BOTH),',', 
	strip(total_premium_am,BOTH),',', 
	strip(tier_cd,BOTH),',', 
	strip(ins_address,BOTH),',',
	strip(ins_city,BOTH),',',
	strip(ins_state,BOTH)
from
 	pol01_policy
where 
   	ins_zip_cd = '65203' AND status_cd = 'A'

